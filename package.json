{
    "name": "noteroom",
    "private": true,
    "workspaces": [
        "backend",
        "frontend"
    ],
    "scripts": {
        "dev:frontend": "npm run dev --workspace=frontend -- --host",
        "dev:backend": "npm run dev --workspace=backend",
        "build:backend": "npm run build --workspace=backend",
        "build:frontend": "npm run build --workspace=frontend"
    },
    "dependencies": {
        "react-icons": "^5.5.0",
        "sharp": "^0.33.5"
    }
}
